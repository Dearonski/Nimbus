.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/

# sourcekit-lsp build config — machine-specific, generated by xcode-build-server
buildServer.json
.compile

# Working notes and the assistant's brief — kept locally, not part of the public repository
docs/
CLAUDE.md
*.p12

# Local Claude Code settings
.claude/
